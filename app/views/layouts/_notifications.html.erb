<% if flash %>
  <%= javascript_tag do %>

    <% flash.keys.each do |msg|
      toastr_msg = msg
      toastr_msg = 'success' if msg == 'notice'
      toastr_msg = 'error' if msg == 'alert'
      %>
      toastr["<%= toastr_msg %>"]("<%= flash[msg] %>");
    <% end %>

  <% end %>
<% end %>
